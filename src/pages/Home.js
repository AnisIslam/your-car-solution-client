import React from 'react';
import Header from '../components/Home/Header/Header';

const Home = () => {
    return (
        <main>
            <Header></Header>
        </main>
    );
};

export default Home;